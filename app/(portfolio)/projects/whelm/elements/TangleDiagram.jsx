import BleedAsset from './BleedAsset'

/* Tangle — Lorin's exported tangle_triangle.svg (slide 255:363).
   Full-bleed knot of curves with embedded labeled lavender nodes.
   Geometry is hers, not approximated. */
export default function TangleDiagram() {
  return (
    <BleedAsset
      src="/marks/whelm/tangle_triangle.svg"
      alt="Overwhelm is a tangle — interconnected emotions and contexts"
    />
  )
}
