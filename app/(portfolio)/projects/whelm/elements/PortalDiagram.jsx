import BleedAsset from './BleedAsset'

/* Portal — Lorin's exported portal.svg (slide 255:365).
   Full-bleed dashed spiral drawing inward to the "Self" core.
   Geometry is hers, not approximated. */
export default function PortalDiagram() {
  return (
    <BleedAsset
      src="/marks/whelm/portal.svg"
      alt="Overwhelm is a portal — a dashed spiral path inward to self"
    />
  )
}
