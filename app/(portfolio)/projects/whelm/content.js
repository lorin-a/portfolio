/* Whelm hero copy — the only copy block driven by the COPY/position
   pipeline. Post-pivot, every other section component owns its copy
   directly (lens claims live inside their section files for clarity).

   Copy verbatim from Figma slide 255:355. */

export const COPY = {
  hero: [
    { position: 'below_center', kind: 'sub', html: '<em>A Ritual for Returning to Yourself</em>' },
  ],
}
