import BuiltFromWords from '@/components/Birthstory/moment/BuiltFromWords'

// Moment study 01 — the screen-test method (F15): one fully-behaving viewport,
// judged live in the browser by feel. Review-only.
export const metadata = {
  title: 'Moment study 01 — Built from their words',
  robots: { index: false, follow: false, nocache: true },
}

export default function MomentOnePage() {
  return <BuiltFromWords />
}
