import VPrint from '@/components/Birthstory/variants/VPrint'

// Direction study D — the case study as a designed print feature: folio
// marks, standfirsts, duotone plates, contact sheets, errata. Review-only.
export const metadata = {
  title: 'Birth Story — direction D · The Print Issue',
  robots: { index: false, follow: false, nocache: true },
}

export default function Page() {
  return <VPrint />
}
