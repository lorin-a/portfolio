import VSkin from '@/components/Birthstory/variants/VSkin'

// Direction study E — the case study told in Birth Story's own design
// system: information rendered as UI. Review-only.
export const metadata = {
  title: 'Birth Story — direction E · The Product Skin',
  robots: { index: false, follow: false, nocache: true },
}

export default function Page() {
  return <VSkin />
}
