import VLedger from '@/components/Birthstory/variants/VLedger'

// Direction study A — the case study as an argument: numbered decisions,
// evidence, and an honest liabilities column. Review-only route.
export const metadata = {
  title: 'Birth Story — direction A · The Ledger',
  robots: { index: false, follow: false, nocache: true },
}

export default function Page() {
  return <VLedger />
}
