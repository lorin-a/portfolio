import VWall from '@/components/Birthstory/variants/VWall'

// Direction study F — the process as material: pinned prints, taped
// printouts, index cards, sticky notes, red pen. Review-only.
export const metadata = {
  title: 'Birth Story — direction F · The Studio Wall',
  robots: { index: false, follow: false, nocache: true },
}

export default function Page() {
  return <VWall />
}
