import VAtlas from '@/components/Birthstory/variants/VAtlas'

// Direction study C — the whole project as one synthesis map: overview first,
// then a guided camera walk with a minimap. Review-only route.
export const metadata = {
  title: 'Birth Story — direction C · The Atlas',
  robots: { index: false, follow: false, nocache: true },
}

export default function Page() {
  return <VAtlas />
}
